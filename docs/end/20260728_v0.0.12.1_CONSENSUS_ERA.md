# IEUM Chain v0.0.12.1

- `ieum:nil` 도메인 값에 서명하는 nil prevote/precommit
- 등록 검증자 투표권 2/3 초과를 검증하는 round-change certificate
- era 길이와 검증자 세트 활성화 지연
- 요청 직후가 아닌 다음 안전한 era 경계에서 검증자 세트 변경

장시간 분리·재결합은 `tests/four_process_network.sh`를 확장해 실제 프로세스와
QUIC 네트워크에서 추가 검증해야 한다.
